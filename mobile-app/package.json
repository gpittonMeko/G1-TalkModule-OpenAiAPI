{
  "name": "g1-talk-remote",
  "version": "1.0.0",
  "description": "G1 Talk Remote - Gestione servizi e soundboard standalone per Unitree G1",
  "scripts": {
    "build": "echo Built",
    "cap:sync": "npx cap sync",
    "cap:open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/core": "^4.6.0",
    "@capacitor/android": "^4.6.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^4.6.0"
  },
  "capacitor": {
    "android": {
      "allowMixedContent": true
    }
  }
}
