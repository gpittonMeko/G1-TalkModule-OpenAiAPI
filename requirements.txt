# G1 Talk Module - Dependencies
# Python 3.10+ | ARM-compatible (Jetson, AI Accelerator, ecc.)
# Robot DDS + LocoClient (Jetson): scripts/install_unitree_sdk_jetson.sh (not on PyPI as a single wheel for aarch64)
#
# Snapshot completo dell'ambiente pip su una macchina: requirements-machine.txt
# (rigenera con scripts/export_dependencies.ps1 o .sh)

openai>=1.0.0
groq>=0.4.0
deepgram-sdk>=3.0.0
sounddevice>=0.4.6
numpy>=1.24.0
python-dotenv>=1.0.0
pydub>=0.25.0
soundfile>=0.12.0
requests>=2.28.0
ddgs>=6.0.0
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
# ffmpeg incluso (Windows/Linux) per WebM browser → WAV verso STT
imageio-ffmpeg>=0.4.9
tqdm>=4.66.0
