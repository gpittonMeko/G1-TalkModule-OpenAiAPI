Origine JSON: config\soundboard.json.from_jetson
Solo traccia clean (audio_base64_clean). Nomi = indice + testo pulsante come in web /client.
Sul robot l'audio resta nel JSON, non in cartelle .wav dedicate.
